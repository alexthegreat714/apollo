# Logs (stub only)

- Real logs stay under `logs/` in the repo root (not archived here).
- Console conversations write JSONL files to `logs/conversations/` via `apollo_console/logging.py`.
- RAG monitor reports append to `logs/rag_monitor.log`.

This stub documents the locations without shipping any production data.
