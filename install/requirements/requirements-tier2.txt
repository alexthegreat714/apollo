# Tier 2 — Mid-range (8GB+ GPU, 16GB+ RAM)
# Same as tier1 — the difference is .env model selection, not packages
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0
torchvision>=0.20.0

transformers>=5.0.0
datasets>=2.0.0
sentencepiece>=0.1.99
timm>=0.9.0

paddlepaddle-gpu>=2.6.1
paddleocr>=2.7.3

magic-pdf[full]>=0.9.0

rapidocr-onnxruntime>=1.3.0
